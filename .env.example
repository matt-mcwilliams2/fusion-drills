DATABASE_URL=postgresql://user:password@localhost:5432/fusionfc
JWT_SECRET=your-secret-key-here
PORT=3001

# Transactional email via Resend (optional — emails queue to DB if not configured)
RESEND_API_KEY=
EMAIL_FROM=noreply@dailyreps.app

# App URL (used in consent/portal email links)
APP_URL=https://dailyreps.app
